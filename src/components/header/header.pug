include ../menu/menu

mixin header({rows, auth, account, logo})
  .header
    .header__nav 
      .header__logo 
        a.header__logo-link(href=logo.href || "./index.html")
          img.header__logo-img(src=logo.src alt=logo.alt || "logo")
      +menu({rows, auth, account})
