include ../nav/nav

mixin header({items, logo})
  .header
    .header__nav 
      .header__logo 
        a.header__logo-link(href=logo.href)
          img.header__logo-img(src=logo.src alt=logo.alt || "logo")
      +nav({items}) 
