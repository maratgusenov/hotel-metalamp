mixin radio({items, title})
  .radio
    if title 
      .radio__title=title
    .radio__buttons    
      each item in items
        - const { type, name, text } = item
          label.radio__label  
            input.radio__input(
              type=type,
              name=name
            )
            span.radio__box
            span.radio__text=text